/* Arrow functions allows a shorter syntax for function expression.
You don't need function keywords, return keywords, and curly rackets.
*/
const subtract = (x, y) => x - y;
console.log(subtract(10, 1));